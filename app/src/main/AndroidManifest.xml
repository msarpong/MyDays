<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.msarpong.mydays">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.detailDate.DetailDate"></activity>
        <activity android:name=".ui.detail.DetailScreen" />
        <activity android:name=".ui.calendar.CalendarScreen" />
        <activity android:name=".ui.setting.SettingScreen" />
        <activity android:name=".ui.add.text.AddTextScreen" />
        <activity android:name=".ui.add.ChoiceScreen" />
        <activity android:name=".ui.splash.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.main.MainScreen" />
    </application>

</manifest>